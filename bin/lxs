#!/bin/bash

NAME=$1

if [ "$1" == "--all" ]; then
	lxc-ls --running | xargs -I%% lxc-stop -n %% &
	exit 0;
fi

if ! lxc-ls | grep ^$NAME\$ > /dev/null; then
	echo "Container \"$NAME\" does not exist"
	exit 1
fi

lxc-stop -n $NAME
